A program that will read an order from a text file, turn that order into a list of products, determine which products can be shipped now and which must be delayed until later, and then produce two reports
